import { PrevdefaultDirective } from './prevdefault.directive';

describe('PrevdefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevdefaultDirective();
    expect(directive).toBeTruthy();
  });
});
